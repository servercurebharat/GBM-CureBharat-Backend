const fetch = require('node-fetch');

(async () => {
  try {
    const saleId = '6a3bd7d9286cb84c1968750c';
    // We need an admin token to make this request.
    // Instead of making a full HTTP request, maybe we can just read the server console?
    // We can't read the server console directly easily.
    // Let's just create a dummy admin token.
  } catch (err) {
    console.error(err);
  }
})();
